# New Zealand HSNO RDF

Reproducible RDF transformation of the `new-zealand-hsno` BioBrick. Each hazard classification is a typed record linked with `IAO_0000136` to a standard compound hub, preferring CAS RN and retaining SMILES. Run `uv run pytest` and `dvc repro`.
